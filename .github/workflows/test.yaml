name: Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    secrets: inherit
    uses: h8io/gha/.github/workflows/test.yaml@v6
    with:
      java-version: 21
      publish-scoverage-summary: false