name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write

jobs:
  release:
    secrets: inherit
    uses: h8io/gha/.github/workflows/release.yaml@v4
    with:
      java-version: 21
      debug: true
      verbose: true
